# TASKS - G_TaskCenter

## Pending
- [ ] Implement user authentication CLI for Gmail and Microsoft Graph (to generate `token.pickle`).
- [ ] Refine the task unification logic to handle duplicate tasks across platforms.
- [ ] Add more sources (e.g., Slack, Jira).
- [ ] Implement data persistence in a local SQLite DB (to handle offline mode).
- [ ] Implement automated tests for all integrations.
- [ ] Configure `GEN_OS` pre-classifier to route task-related queries to the `g-taskcenter` server.
